part of 'dj_home.dart';

class DjHomeScreen extends StatelessWidget {
  const DjHomeScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
