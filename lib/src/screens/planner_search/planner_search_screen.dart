part of 'planner_search.dart';

class PlannerSearchScreen extends StatelessWidget {
  const PlannerSearchScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
