part of '../family.dart';

class FamilyScreen extends StatelessWidget {
  const FamilyScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
